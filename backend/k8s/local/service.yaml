apiVersion: v1
kind: Service
metadata:
  name: taskline-backend-service
  labels:
    app: taskline-backend
spec:
  type: NodePort
  selector:
    app: taskline-backend
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30050