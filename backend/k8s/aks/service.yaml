apiVersion: v1
kind: Service
metadata:
  name: taskline-backend-service
  labels:
    app: taskline-backend
spec:
  type: ClusterIP
  selector:
    app: taskline-backend
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
